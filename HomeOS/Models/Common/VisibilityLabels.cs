namespace HomeOS.Models.Common;

// Marker for localizing the Visibility enum's user-facing labels
// (VisibilityLabels.{culture}.resx). Shared because Visibility is a
// cross-module Common concept, not owned by a single module.
public class VisibilityLabels
{
}
