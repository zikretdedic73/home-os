namespace HomeOS.Models.Finance;

// Marker type so views can inject IStringLocalizer<FinanceEnums> and localize
// enum labels by key (e.g. "TransactionType_Expense") - same pattern as
// TaskEnums / VisibilityLabels.
public class FinanceEnums
{
}
