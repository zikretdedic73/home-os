namespace HomeOS.Models.Tasks;

// Marker type only - gives IStringLocalizer<TaskEnums> a stable name to
// resolve against Models/Tasks/TaskEnums.{culture}.resx, so TaskState and
// TaskPriority display text can be localized wherever they are rendered.
public class TaskEnums
{
}
